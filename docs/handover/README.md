# Handovers

One file per session that changed state worth passing on, named
`YYYY-MM-DD-<topic>.md`. Keep each under a page:

1. What was done (files, commands, results).
2. State of long-running things (downloads, servers, background jobs).
3. What is next, in order, pointing at the brief to execute.
4. Surprises and decisions taken on the way (link an ADR if it matters).

Add a line to the index below in the same commit. The index is chronological,
oldest first.

## Index

| File | What it passes on |
|---|---|
| `2026-09-13-bootstrap.md` | Phase 0: repo, licence, research, decisions, `PLAN.md`, `DATA-SCHEMA.md`, the three briefs. How the stream download was started and how to check it. |
| `2026-09-13-data.md` | Workstream B first increment: `data/schema/class.schema.json`, `pipeline/validate.py` (rules 1-19 and `canonical_dumps`, the one serializer), encoding v1, the tinker example, the Classic Era prior (432 talents). |
| `2026-09-13-llama-build.md` | llama.cpp built with CUDA (release b10931) plus the model-download and `llama-server` scripts. Build flags, verification, and why the server runs on port 8089. |
| `2026-09-13-web.md` | Workstream C, milestones M1 **and** M2: rules engine, hash routing, Pages deploy, then crop icons, class picker, `#/review/<class>` and document titles. The most complete description of the web architecture outside the brief. |
| `2026-09-13-pipeline-probe.md` | Stage 0 against the live fragment URL while the download ran: 28 talent segments (03:47:20-06:17, all nine classes) and the pixel-exact measurement of the Forever talent UI that every later stage's layout prior comes from. |
| `2026-09-13-data-ranks-export.md` | Stages 6 and 8: `ranks.py` (Classic match plus slot scaling) and `export.py` (`build_extracted`, `apply_overrides`, `build_talents`, `update_encoding`, `write_validated`). Read this before changing how a class file is produced. |
| `2026-09-13-pipeline-hovers.md` | Stages 3 and 4: grid calibration from Sobel square-outline search, ffmpeg frame decoding, tooltip blob detection, tab state. The CV foundations. |
| `2026-09-13-paladin-e2e.md` | First class through the whole pipeline (45 of 52 talents). Documents the VLM prompt, the two-pass reader contract, the confidence model and the codex second opinion — still the reference for all of those. |
| `2026-09-13-all-classes.md` | Phase 2: all nine classes extracted (434 of 470 at the time), per-class timings, runner fixes, and the coverage table in `data/extracted/SUMMARY.md`. |
| `2026-09-13-missing-cells-forensics.md` | Why 36 cells looked "never hovered": dark-panel tooltips under the area gate, ghost tooltips baked into the median, merged blobs. 34 recovered, detector fixed generally, and the proof that only mage Fire r1c3 is genuinely absent from the footage. |
| `2026-09-13-prerequisites.md` | Prerequisite arrows read from the tree backgrounds (67 across 27 trees) because rank-0 tooltips never name a prerequisite. Ridge masks, path templates, the arrowhead test and the confidence vote. |
| `2026-09-13-rank-scaling.md` | The proportional-scaling rule for anticipated ranks (Meditation 17/34/51, not 17/22/27), the classification of Classic progressions, and the 63 talents it corrected. The addendum (section 6) makes `v1 * k` the default for every talent, keeps only the exact-name verbatim copy, and fixes Twilight Focus 23/46/69. Cross-referenced from `DATA-SCHEMA.md` section 5 and `pipeline/README.md`. |
| `2026-09-13-icons.md` | Stage 9: 397 talents matched to clean Classic icon files by NCC plus pHash, 72 kept as crops (24 of them likely new Forever icons). Matching thresholds, the reference set, and how to re-apply. |
| `2026-09-13-fix-c-ci-docs.md` | Review work package C: the CI `check` job, the CLAUDE.md rewrite, the `DATA-SCHEMA.md` corrections, the `PLAN.md` refresh and this index. |
| `2026-09-13-fix-a1-tooltip.md` | Work package A1: one amber trust line plus a Details disclosure in the tooltip, requirement lines from `source.note`, the pure text helpers in `web/src/ui/tooltipText.ts` and their corpus tests, review-route filters and diffs, picker copy. Requests for the A2 owner. |
| `2026-09-13-fix-a2-interaction.md` | Work package A2: keyboard refund and the WAI grid pattern, the touch tooltip with +/- controls, blocked-action messages, the zero-points state, talent search, crop-icon clipping, contrast and landmarks, plus the build-time `classes-index.json`, the code-split crop registry and zod off the production path (-34.7 kB gzip on every first load). |
| `2026-09-13-fix-b-pipeline-data.md` | Review work package B: the shape-aware merge of the two tooltip readers (review queue 77 -> 6), the 53 audit overrides and the 5 decisions, atomic writes and one slug rule, the `frozen` encoding flag, override `unset`, rank-sanity rule 20, and the orphaned-crop prune. Read this before changing how a reading becomes a talent record. |
| `2026-09-13-nested-tooltips.md` | Crusader Kings style sticky, nested tooltips: the dwell-plus-approach rule that lets the tooltip layer take the pointer without making the covered cell unclickable, the placement rule, the terms that replaced the `<details>` disclosure (derivation, Classic rank values, prerequisite card, both readings with the source crop), `d` as the no-pointer fallback. Closes A1's request 1 to A2. |
| `2026-09-13-ui-round1b.md` | UI round 1b: the build-time Classic Era diff (`web/src/data/classic-diff.json`, 145 new / 132 moved / 18 re-ranked / 108 gone), the new-in-Forever star and its one tooltip line, the "what's new" switch in the tree panel header, shift-click max and ctrl/alt clear driven one point per render through the existing verdicts, and the tier gutter, honest arrows and readable locked cells. |
| `2026-09-13-ui-round1a.md` | UI round 1a: the build summary column with the Discord text export, bare build code and copy link; the wide-desktop trees-plus-summary grid with 56px cells and level tree panels; the nine-class switcher in the header; an undo/redo reducer with one browser-history entry per editing session and an undoable Reset; the localStorage "Continue" card; and import from our link, our build code or a Wowhead Classic string, whose digit order is verified against three real labelled builds. |
| `2026-09-13-ui-polish-icons.md` | The nine `classicon_*.jpg` files (fetched by hand, documented in `web/public/icons/CLASS-ICONS.md`, **not** stage 9 output) behind the switcher chips and the landing cards; the 18px "New in Forever" star; the summary column's empty state as the shortcut card; nothing player-facing below 12px; and one class-level control row - search, What's new, Reset - in one style. |
| `2026-09-13-classic-diff-v2.md` | The Classic Era diff, round 2: renamed trees (Shadow -> Shadow Magic) matched before cells are compared, so `moved` means moved - 132 -> 123, with exactly eight genuine cross-tree moves; rank-1 descriptions compared through a unit-aware normaliser, giving `text-changed` (111) and `values-changed` (16); the precise change line (`Reworked.`, `Values changed: 15% -> 20%.`) and the nested card that shows the Classic wording with the word diff drawn into it, from a lazily fetched `classic-text.json`. |
| `2026-09-13-horizontal-prereqs-web.md` | Same-row prerequisites on the web (priest Improved Mind Flay requires Mind Flay next to it): the relaxed `requires` rule and the cycle check in the Zod mirror, table-driven rules tests for the horizontal case, the arrow geometry pulled out of `Arrows.tsx` into `arrows.ts` with left-to-right and right-to-left lines that keep the head in the gutter, and the `fixture-arrows` fixture class behind them. |
| `2026-09-13-cell-attribution-audit.md` | The audit that cleared stage 4: 115 same-tree "moved" talents checked against the icon in the median at the recorded cell, a swap test over all 324 comparable cells, the corner-distance statistics of all 646 hovers and six frames decoded from the mkv. Priest Blackout and Spirit Tap really did trade places. The wording was the bug, so a column-only change is no longer a move (`moved` 123 -> 81) and the line names both rows. Also: same-row prerequisites allowed by rule 8, and the two horizontal arrows stage 7 now finds. |
